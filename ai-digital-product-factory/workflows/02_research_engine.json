{
  "name": "ADPF 02 Research Engine",
  "id": "ADPF02RESEARCHENGN",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "adpf02-receive-research-request",
      "name": "Receive Research Request",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        0,
        190
      ],
      "notes": "Entry point. Called by 01 Master Controller with the run context.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// =====================================================================\n// FACTORY CONFIG - ศูนย์รวมค่าตั้งค่าของโรงงานทั้งหมด\n//\n// วิธีแก้ค่า (เลือกอย่างใดอย่างหนึ่ง):\n//   1. แก้ตัวเลข/ข้อความใน DEFAULTS ข้างล่างนี้ตรง ๆ (ง่ายสุด ใช้ได้ทุกแพลน)\n//   2. หรือตั้งใน n8n Variables (Settings > Variables) ชื่อเดียวกัน - จะทับ DEFAULTS\n//   3. หรือตั้ง environment variable (เฉพาะ self-hosted) - จะทับ DEFAULTS เช่นกัน\n//\n// แก้ที่ workflow 01 ตัวเดียวพอ: sub-workflow ทุกตัวรับ cfg ต่อจากแม่อัตโนมัติ\n// =====================================================================\nconst DEFAULTS = {\n  \"OPENAI_MODEL_TEXT\": \"gpt-4.1\",\n  \"OPENAI_MODEL_IMAGE\": \"gpt-image-1\",\n  \"OPENAI_IMAGE_QUALITY\": \"high\",\n  \"PROMPT_VERSION\": \"v1.0.0\",\n  \"ETSY_API_BASE\": \"https://openapi.etsy.com\",\n  \"ETSY_API_KEY\": \"\",\n  \"ETSY_SHOP_ID\": \"\",\n  \"ETSY_LISTING_STATE\": \"draft\",\n  \"ETSY_TAXONOMY_ID\": 2078,\n  \"GDRIVE_ROOT_FOLDER_ID\": \"root\",\n  \"DB_TYPE\": \"postgres\",\n  \"DB_SQLITE_PATH\": \"/home/node/.n8n/adpf.db\",\n  \"FACTORY_ROTATION\": \"planner,printable,canva,wallart,resume,spreadsheet,kids,svg\",\n  \"ALERT_WEBHOOK_URL\": \"\",\n  \"IDEA_TARGET_COUNT\": 200,\n  \"IDEA_BATCH_SIZE\": 25,\n  \"DUPLICATE_SIMILARITY_THRESHOLD\": 0.62,\n  \"ALLOW_PRODUCT_REVISION\": \"false\",\n  \"BUDGET_DAILY_USD\": 20,\n  \"BUDGET_MONTHLY_USD\": 300,\n  \"BUDGET_RUN_RESERVE_USD\": 3,\n  \"OPENAI_IMAGE_UNIT_COST_USD\": 0.19,\n  \"OPENAI_CHAT_RUN_COST_USD\": 0.12,\n  \"OPENAI_PRICE_OVERRIDE_JSON\": \"{}\",\n  \"LEARNING_WINDOW_DAYS\": 30,\n  \"LEARNING_MIN_SAMPLE\": 15,\n  \"AB_MIN_VIEWS_PER_ARM\": 200,\n  \"AB_MIN_RELATIVE_LIFT\": 0.2\n};\n\nconst first = $input.first();\nconst incoming = first && first.json ? first.json : {};\nconst parentCfg = incoming.cfg && typeof incoming.cfg === 'object' ? incoming.cfg : {};\n\n// ทั้ง $vars และ $env อาจไม่มีอยู่เลย (n8n Cloud) - แตะผ่าน try เท่านั้น\nconst readVars = (key) => {\n  try {\n    if (typeof $vars !== 'undefined' && $vars[key] !== undefined && $vars[key] !== '') {\n      return $vars[key];\n    }\n  } catch (e) { /* Variables not available on this plan */ }\n  return undefined;\n};\nconst readEnv = (key) => {\n  try {\n    if (typeof $env !== 'undefined' && $env[key] !== undefined && $env[key] !== '') {\n      return $env[key];\n    }\n  } catch (e) { /* env access blocked or not present */ }\n  return undefined;\n};\n\nconst cfg = {};\nconst sources = {};\nfor (const key of Object.keys(DEFAULTS)) {\n  if (parentCfg[key] !== undefined) { cfg[key] = parentCfg[key]; sources[key] = 'parent'; continue; }\n  const fromVars = readVars(key);\n  if (fromVars !== undefined) { cfg[key] = fromVars; sources[key] = 'variables'; continue; }\n  const fromEnv = readEnv(key);\n  if (fromEnv !== undefined) { cfg[key] = fromEnv; sources[key] = 'env'; continue; }\n  cfg[key] = DEFAULTS[key]; sources[key] = 'default';\n}\n\nreturn $input.all().map((item) => ({\n  json: { ...item.json, cfg, cfg_sources: sources },\n  binary: item.binary,\n}));\n"
      },
      "id": "adpf-adpf02researchengn-factory-config",
      "name": "Factory Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        150,
        60
      ],
      "notes": "แก้ค่าตั้งค่าทั้งหมดที่ DEFAULTS ในโค้ดของ node นี้ (ดับเบิลคลิกเข้ามาแก้ได้เลย) รองรับ n8n Cloud - ไม่ต้องใช้ environment variable. Order: parent cfg > Variables > env > defaults.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Central prompt library. Every OpenAI agent used by this workflow lives\n// here so prompts are versioned with the workflow itself and can be\n// diffed in git (mirrored in prompts/*.md).\nconst PROMPT_VERSION = $('Factory Config').first().json.cfg.PROMPT_VERSION || 'v1.0.0';\nconst PROMPTS = {\n  \"research_ai\": {\n    \"id\": \"research_ai\",\n    \"title\": \"Research AI\",\n    \"purpose\": \"Turn raw Etsy marketplace signals into one validated product concept.\",\n    \"system\": \"You are Research AI, a senior Etsy marketplace analyst for a digital product studio.\\nYou only analyse digital (instant download) products. You never invent statistics:\\nevery number you output must be derived from the MARKET SIGNALS block supplied by\\nthe user, or explicitly marked as an estimate inside `_estimates`.\\n\\nMethod, in order:\\n1. Read the market signals (titles, tags, prices, listing ages, competitor counts).\\n2. Group listings into micro-niches and score each one on demand vs. saturation.\\n3. Reject niches that need trademarks, licensed characters, or real brand names.\\n4. Reject niches that a solo studio cannot produce as a printable/digital file.\\n5. Pick the single best remaining micro-niche and describe it precisely.\\n\\nRules:\\n- `keyword` must be a real buyer search phrase, 2-5 words, lowercase.\\n- `sub_keywords` must be 8-15 distinct long-tail phrases, lowercase, no duplicates,\\n  each <= 20 characters so they can be reused as Etsy tags.\\n- `difficulty` is one of \\\"easy\\\" | \\\"medium\\\" | \\\"hard\\\" and reflects competition, not\\n  production effort.\\n- `selling_points` are buyer-facing benefits, not feature lists.\\n- Output valid JSON only. No markdown, no commentary.\",\n    \"user_template\": \"FACTORY: {{factory}}\\nRUN ID: {{run_id}}\\nSEED KEYWORD: {{seed_keyword}}\\n\\nMARKET SIGNALS (from Etsy Open API v3):\\n{{market_signals_json}}\\n\\nProduce one product concept for the {{factory}} factory that fits the market gap\\nyou identified. Answer with the exact JSON contract.\",\n    \"output_contract\": \"{\\n  \\\"product_type\\\": \\\"string\\\",\\n  \\\"category\\\": \\\"string\\\",\\n  \\\"target_customer\\\": \\\"string\\\",\\n  \\\"keyword\\\": \\\"string\\\",\\n  \\\"sub_keywords\\\": [\\\"string\\\"],\\n  \\\"difficulty\\\": \\\"easy|medium|hard\\\",\\n  \\\"selling_points\\\": [\\\"string\\\"],\\n  \\\"price_suggestion_usd\\\": 0,\\n  \\\"market_gap\\\": \\\"string\\\",\\n  \\\"rejected_ideas\\\": [\\\"string\\\"]\\n}\"\n  },\n  \"seo_ai\": {\n    \"id\": \"seo_ai\",\n    \"title\": \"SEO AI\",\n    \"purpose\": \"Produce Etsy-compliant title, description and tags.\",\n    \"system\": \"You are SEO AI, an Etsy search optimisation specialist.\\n\\nHard Etsy limits you must never break:\\n- Title: max 140 characters, primary keyword inside the first 40 characters,\\n  no ALL CAPS words, no more than one \\\"|\\\" separator group per phrase.\\n- Tags: exactly 13 tags, each max 20 characters, lowercase, letters/numbers/spaces\\n  only, no duplicates, no single-word repeats of the same stem more than twice.\\n- Description: 5 short paragraphs - hook, what's included, how to use it,\\n  file/delivery details, policy note that it is an instant digital download with\\n  no physical item shipped.\\n- Never promise refunds, never mention competitors, never use trademarked terms.\\n- Output valid JSON only.\",\n    \"user_template\": \"TASK: {{seo_task}}\\n\\nRESEARCH CONCEPT:\\n{{research_json}}\\n\\nPRODUCT BRIEF:\\n{{idea_json}}\\n\\nDELIVERABLE FILES:\\n{{file_manifest}}\\n\\nAnswer with the exact JSON contract for this task.\",\n    \"output_contract\": \"title  -> {\\\"title\\\": \\\"string\\\", \\\"char_count\\\": 0}\\ndescription -> {\\\"description\\\": \\\"string\\\", \\\"paragraph_count\\\": 0}\\ntags -> {\\\"tags\\\": [\\\"string\\\"], \\\"materials\\\": [\\\"string\\\"]}\\nkeywords -> {\\\"primary\\\": \\\"string\\\", \\\"sub_keywords\\\": [\\\"string\\\"], \\\"long_tail\\\": [\\\"string\\\"]}\"\n  }\n};\n\nreturn $input.all().map((item) => ({\n  json: { ...item.json, prompt_version: PROMPT_VERSION, prompts: PROMPTS },\n}));\n"
      },
      "id": "adpf02-prompt-library",
      "name": "Prompt Library",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        300,
        190
      ],
      "notes": "Versioned prompts for the two agents used in this workflow.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Decide what to research. The master controller may pass a seed keyword; when it\n// does not, each factory has a sensible default search phrase.\nconst FACTORY_SEEDS = {\n  planner: 'digital planner printable',\n  printable: 'printable wall planner',\n  canva: 'canva template editable',\n  wallart: 'printable wall art set',\n  resume: 'resume template modern',\n  spreadsheet: 'budget spreadsheet template',\n  kids: 'kids printable activity',\n  svg: 'svg cut file bundle',\n};\n\nconst ctx = $input.first().json;\nconst factory = String(ctx.factory || 'printable').toLowerCase();\nconst seed = (ctx.seed_keyword || '').trim() || FACTORY_SEEDS[factory] || 'printable digital download';\n\nreturn [{\n  json: {\n    ...ctx,\n    factory,\n    search_keyword: seed,\n    etsy_api_base: ctx.etsy_api_base || $('Factory Config').first().json.cfg.ETSY_API_BASE,\n    researched_at: new Date().toISOString(),\n  },\n}];"
      },
      "id": "adpf02-normalize-research-input",
      "name": "Normalize Research Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        600,
        190
      ],
      "notes": "Resolves the search keyword (explicit seed or per-factory default).",
      "notesInFlow": true
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Factory Config').first().json.cfg.ETSY_API_BASE }}/v3/application/listings/active?limit=100&sort_on=score&sort_order=desc&keywords={{ encodeURIComponent($json.search_keyword) }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-api-key",
              "value": "={{ $('Factory Config').first().json.cfg.ETSY_API_KEY }}"
            }
          ]
        },
        "options": {
          "timeout": 120000
        }
      },
      "id": "adpf02-etsy-search-active-listings",
      "name": "Etsy: Search Active Listings",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        900,
        190
      ],
      "notes": "Live competitor sample for the target keyword. Degrades gracefully: on error the run continues with whatever signals are available.",
      "notesInFlow": true,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Factory Config').first().json.cfg.ETSY_API_BASE }}/v3/application/seller-taxonomy/nodes",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-api-key",
              "value": "={{ $('Factory Config').first().json.cfg.ETSY_API_KEY }}"
            }
          ]
        },
        "options": {
          "timeout": 120000
        }
      },
      "id": "adpf02-etsy-fetch-taxonomy-nodes",
      "name": "Etsy: Fetch Taxonomy Nodes",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1200,
        190
      ],
      "notes": "Official Etsy category tree, used to ground the category suggestion.",
      "notesInFlow": true,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Turn the raw Etsy responses into compact, quantified market signals. Sending\n// 100 full listings to the model is wasteful and noisy - sending aggregates is\n// cheaper and produces far more grounded answers.\nconst ctx = $('Normalize Research Input').first().json;\n\nconst safe = (nodeName, path) => {\n  try {\n    const json = $(nodeName).first().json;\n    return path(json) || [];\n  } catch (e) {\n    return [];\n  }\n};\n\nconst listings = safe('Etsy: Search Active Listings', (j) => j.results);\nconst taxonomy = safe('Etsy: Fetch Taxonomy Nodes', (j) => j.results);\n\nconst STOPWORDS = new Set([\n  'the', 'and', 'for', 'with', 'your', 'you', 'this', 'that', 'from', 'set',\n  'pack', 'pdf', 'png', 'digital', 'download', 'instant', 'printable', 'file',\n  'files', 'template', 'templates', 'a', 'of', 'to', 'in', 'on', 'by',\n]);\n\nconst prices = [];\nconst tagCount = new Map();\nconst titleCount = new Map();\nlet totalSales = 0;\nlet withSales = 0;\n\nfor (const listing of listings) {\n  const amount = listing?.price?.amount;\n  const divisor = listing?.price?.divisor || 100;\n  if (typeof amount === 'number' && divisor) prices.push(amount / divisor);\n\n  if (typeof listing?.num_favorers === 'number') {\n    totalSales += listing.num_favorers;\n    withSales += 1;\n  }\n\n  for (const tag of listing?.tags || []) {\n    const key = String(tag).toLowerCase().trim();\n    if (key) tagCount.set(key, (tagCount.get(key) || 0) + 1);\n  }\n\n  for (const word of String(listing?.title || '').toLowerCase().split(/[^a-z0-9]+/)) {\n    if (word.length < 3 || STOPWORDS.has(word)) continue;\n    titleCount.set(word, (titleCount.get(word) || 0) + 1);\n  }\n}\n\nconst top = (map, n) => [...map.entries()]\n  .sort((a, b) => b[1] - a[1])\n  .slice(0, n)\n  .map(([value, count]) => ({ value, count }));\n\nprices.sort((a, b) => a - b);\nconst percentile = (p) => (prices.length ? prices[Math.min(prices.length - 1, Math.floor(prices.length * p))] : null);\nconst round2 = (n) => (n === null ? null : Math.round(n * 100) / 100);\n\nconst signals = {\n  search_keyword: ctx.search_keyword,\n  factory: ctx.factory,\n  competitor_count: listings.length,\n  price_usd: {\n    min: round2(prices[0] ?? null),\n    p25: round2(percentile(0.25)),\n    median: round2(percentile(0.5)),\n    p75: round2(percentile(0.75)),\n    max: round2(prices[prices.length - 1] ?? null),\n  },\n  average_favourites: withSales ? Math.round(totalSales / withSales) : null,\n  top_tags: top(tagCount, 25),\n  top_title_words: top(titleCount, 25),\n  etsy_categories: taxonomy\n    .filter((n) => /craft|paper|party|art|digital|home/i.test(n?.name || ''))\n    .slice(0, 25)\n    .map((n) => ({ id: n.id, name: n.name, level: n.level })),\n  sample_titles: listings.slice(0, 15).map((l) => String(l?.title || '').slice(0, 120)),\n  signal_quality: listings.length >= 20 ? 'good' : (listings.length ? 'thin' : 'unavailable'),\n};\n\nreturn [{ json: { ...ctx, market: signals } }];"
      },
      "id": "adpf02-aggregate-etsy-market-signals",
      "name": "Aggregate Etsy Market Signals",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1500,
        190
      ],
      "notes": "Price percentiles, tag/keyword frequency, competitor count, category hints.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Mustache-style renderer for the {{placeholders}} in the prompt templates.\nconst render = (template, vars) =>\n  String(template).replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, (_, key) =>\n    vars[key] === undefined || vars[key] === null ? '' : String(vars[key]));\n\nconst ctx = $input.first().json;\nconst agent = $('Prompt Library').first().json.prompts.research_ai;\n\nconst userPrompt = render(agent.user_template, {\n  factory: ctx.factory,\n  run_id: ctx.run_id,\n  seed_keyword: ctx.search_keyword,\n  market_signals_json: JSON.stringify(ctx.market, null, 2),\n});\n\nreturn [{ json: { ...ctx, research_user_prompt: userPrompt } }];"
      },
      "id": "adpf02-build-research-prompt",
      "name": "Build Research Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1800,
        190
      ],
      "notes": "Renders the Research AI user template with the aggregated signals.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.openai.com/v1/chat/completions",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "openAiApi",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: $('Factory Config').first().json.cfg.OPENAI_MODEL_TEXT || 'gpt-4.1', temperature: 0.6, max_tokens: 2000, response_format: { type: 'json_object' }, messages: [ { role: 'system', content: ($('Prompt Library').first().json.prompts.research_ai.system + '\\n\\nOUTPUT CONTRACT (return exactly this JSON shape):\\n' + $('Prompt Library').first().json.prompts.research_ai.output_contract) }, { role: 'user', content: $json.research_user_prompt } ] }) }}",
        "options": {
          "timeout": 180000
        }
      },
      "id": "adpf02-openai-research-ai",
      "name": "OpenAI: Research AI",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2100,
        190
      ],
      "notes": "Agent 1/8. Chat Completions with response_format=json_object. OpenAI credential only.",
      "notesInFlow": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Parse + validate the model output against the fixed research contract. The\n// node never throws: an invalid answer is routed to the deterministic fallback\n// instead of killing the whole run.\nconst REQUIRED = [\n  'product_type', 'category', 'target_customer', 'keyword',\n  'sub_keywords', 'difficulty', 'selling_points',\n];\n\nconst ctx = $('Build Research Prompt').first().json;\nconst response = $input.first().json;\n\nlet research = null;\nconst errors = [];\n\ntry {\n  const content = response?.choices?.[0]?.message?.content;\n  if (!content) throw new Error('OpenAI returned an empty completion');\n  research = JSON.parse(content);\n} catch (e) {\n  errors.push(`parse: ${e.message}`);\n}\n\nif (research) {\n  for (const key of REQUIRED) {\n    if (research[key] === undefined || research[key] === null || research[key] === '') {\n      errors.push(`missing field: ${key}`);\n    }\n  }\n  if (!Array.isArray(research.sub_keywords) || research.sub_keywords.length < 5) {\n    errors.push('sub_keywords must contain at least 5 entries');\n  }\n  if (!Array.isArray(research.selling_points) || research.selling_points.length < 3) {\n    errors.push('selling_points must contain at least 3 entries');\n  }\n  if (!['easy', 'medium', 'hard'].includes(String(research.difficulty || '').toLowerCase())) {\n    errors.push('difficulty must be easy, medium or hard');\n  }\n}\n\nreturn [{\n  json: {\n    ...ctx,\n    research,\n    valid: errors.length === 0,\n    validation_errors: errors,\n    openai_usage: response?.usage || null,\n  },\n}];"
      },
      "id": "adpf02-parse-research-json",
      "name": "Parse Research JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2400,
        190
      ],
      "notes": "Validates the 7 required contract fields. Never throws - sets valid=false.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "cond-1",
              "leftValue": "={{ $json.valid }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "id": "adpf02-check-research-valid",
      "name": "Check: Research Valid",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2700,
        190
      ],
      "notes": "Valid output continues; anything else falls back to the deterministic concept.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Deterministic safety net so a bad completion degrades the run instead of\n// failing it. Built purely from the aggregated Etsy signals.\nconst ctx = $input.first().json;\nconst market = ctx.market || {};\nconst tags = (market.top_tags || []).map((t) => t.value);\nconst words = (market.top_title_words || []).map((t) => t.value);\n\nconst keyword = (ctx.search_keyword || 'printable digital download').toLowerCase();\nconst subKeywords = [...new Set([...tags, ...words])]\n  .filter((t) => t.length <= 20)\n  .slice(0, 13);\n\nreturn [{\n  json: {\n    ...ctx,\n    research: {\n      product_type: ctx.factory,\n      category: 'Digital Downloads',\n      target_customer: 'Etsy shoppers looking for an instant, ready-to-print digital file',\n      keyword,\n      sub_keywords: subKeywords.length ? subKeywords : [keyword],\n      difficulty: (market.competitor_count || 0) > 60 ? 'hard' : 'medium',\n      selling_points: [\n        'Instant download - no waiting and no shipping',\n        'Print at home or at any print shop',\n        'Clean, modern layout that works on every device',\n      ],\n      price_suggestion_usd: market?.price_usd?.median || 5,\n      market_gap: 'Fallback concept generated from marketplace aggregates only',\n      rejected_ideas: [],\n    },\n    valid: true,\n    used_fallback: true,\n    fallback_reason: (ctx.validation_errors || []).join('; ') || 'model output rejected',\n  },\n}];"
      },
      "id": "adpf02-research-fallback-concept",
      "name": "Research Fallback Concept",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2700,
        380
      ],
      "notes": "Safety net built from Etsy aggregates so a bad completion only degrades the run.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Mustache-style renderer for the {{placeholders}} in the prompt templates.\nconst render = (template, vars) =>\n  String(template).replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, (_, key) =>\n    vars[key] === undefined || vars[key] === null ? '' : String(vars[key]));\n\nconst ctx = $input.first().json;\nconst agent = $('Prompt Library').first().json.prompts.seo_ai;\n\nconst userPrompt = render(agent.user_template, {\n  seo_task: 'keywords',\n  research_json: JSON.stringify(ctx.research, null, 2),\n  idea_json: '(not generated yet - this is the research stage)',\n  file_manifest: '(not generated yet - this is the research stage)',\n});\n\nreturn [{ json: { ...ctx, seo_user_prompt: userPrompt } }];"
      },
      "id": "adpf02-build-keyword-expansion-prompt",
      "name": "Build Keyword Expansion Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3000,
        190
      ],
      "notes": "Renders the SEO AI template in 'keywords' mode.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.openai.com/v1/chat/completions",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "openAiApi",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: $('Factory Config').first().json.cfg.OPENAI_MODEL_TEXT || 'gpt-4.1', temperature: 0.5, max_tokens: 1200, response_format: { type: 'json_object' }, messages: [ { role: 'system', content: ($('Prompt Library').first().json.prompts.seo_ai.system + '\\n\\nOUTPUT CONTRACT (return exactly this JSON shape):\\n' + $('Prompt Library').first().json.prompts.seo_ai.output_contract) }, { role: 'user', content: $json.seo_user_prompt } ] }) }}",
        "options": {
          "timeout": 180000
        }
      },
      "id": "adpf02-openai-seo-ai-keyword-expansion",
      "name": "OpenAI: SEO AI Keyword Expansion",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3300,
        190
      ],
      "notes": "Agent 5/8 (keywords mode). Expands the concept into Etsy-safe long-tail phrases.",
      "notesInFlow": true,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Final research contract returned to 01 Master Controller.\nconst ctx = $('Build Keyword Expansion Prompt').first().json;\nconst response = $input.first().json;\n\nlet expansion = {};\ntry {\n  expansion = JSON.parse(response?.choices?.[0]?.message?.content || '{}');\n} catch (e) {\n  expansion = {};\n}\n\nconst research = ctx.research || {};\nconst clean = (list) => [...new Set(\n  (list || [])\n    .map((s) => String(s).toLowerCase().replace(/[^a-z0-9 ]/g, '').trim())\n    .filter((s) => s && s.length <= 20)\n)];\n\nconst subKeywords = clean([...(research.sub_keywords || []), ...(expansion.sub_keywords || [])]).slice(0, 15);\n\nreturn [{\n  json: {\n    ok: true,\n    run_id: ctx.run_id,\n    factory: ctx.factory,\n    prompt_version: ctx.prompt_version,\n    used_fallback: ctx.used_fallback === true,\n    research: {\n      product_type: research.product_type,\n      category: research.category,\n      target_customer: research.target_customer,\n      keyword: String(research.keyword || ctx.search_keyword).toLowerCase(),\n      sub_keywords: subKeywords,\n      difficulty: String(research.difficulty || 'medium').toLowerCase(),\n      selling_points: research.selling_points || [],\n      price_suggestion_usd: research.price_suggestion_usd || ctx.market?.price_usd?.median || 5,\n      market_gap: research.market_gap || null,\n      long_tail: clean(expansion.long_tail || []).slice(0, 15),\n    },\n    market: ctx.market || {},\n    researched_at: ctx.researched_at,\n  },\n}];"
      },
      "id": "adpf02-build-research-output",
      "name": "Build Research Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3600,
        190
      ],
      "notes": "Emits the fixed research contract consumed by 03 Product Engine.",
      "notesInFlow": true
    },
    {
      "parameters": {},
      "id": "adpf02-return-research-package",
      "name": "Return: Research Package",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        3900,
        190
      ],
      "notes": "Terminal node - its item is the sub-workflow return value.",
      "notesInFlow": true
    }
  ],
  "connections": {
    "Receive Research Request": {
      "main": [
        [
          {
            "node": "Factory Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prompt Library": {
      "main": [
        [
          {
            "node": "Normalize Research Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Research Input": {
      "main": [
        [
          {
            "node": "Etsy: Search Active Listings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Etsy: Search Active Listings": {
      "main": [
        [
          {
            "node": "Etsy: Fetch Taxonomy Nodes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Etsy: Fetch Taxonomy Nodes": {
      "main": [
        [
          {
            "node": "Aggregate Etsy Market Signals",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Aggregate Etsy Market Signals": {
      "main": [
        [
          {
            "node": "Build Research Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Research Prompt": {
      "main": [
        [
          {
            "node": "OpenAI: Research AI",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenAI: Research AI": {
      "main": [
        [
          {
            "node": "Parse Research JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Research JSON": {
      "main": [
        [
          {
            "node": "Check: Research Valid",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check: Research Valid": {
      "main": [
        [
          {
            "node": "Build Keyword Expansion Prompt",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Research Fallback Concept",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Research Fallback Concept": {
      "main": [
        [
          {
            "node": "Build Keyword Expansion Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Keyword Expansion Prompt": {
      "main": [
        [
          {
            "node": "OpenAI: SEO AI Keyword Expansion",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenAI: SEO AI Keyword Expansion": {
      "main": [
        [
          {
            "node": "Build Research Output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Research Output": {
      "main": [
        [
          {
            "node": "Return: Research Package",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Factory Config": {
      "main": [
        [
          {
            "node": "Prompt Library",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveExecutionProgress": true,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "executionTimeout": 3600,
    "timezone": "Asia/Bangkok"
  },
  "pinData": {},
  "staticData": null,
  "meta": {
    "instanceId": "ai-digital-product-factory"
  },
  "tags": [
    {
      "name": "ai-digital-product-factory"
    }
  ],
  "versionId": "v1.0.0"
}
