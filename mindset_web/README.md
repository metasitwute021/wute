# สังเกตการณ์ — เว็บทดสอบ mindset แบบแตกกิ่ง

โฟลเดอร์นี้ **ไม่เกี่ยวกับ EA เทรด** ในโฟลเดอร์หลัก — เป็นโปรเจกต์แยก

เว็บทดสอบนิสัย/mindset ที่ไม่มีชุดคำถามตายตัว คำตอบแต่ละข้อพาไปเจอคำถามคนละชุด
แตกกิ่งจากคลัง 120 ข้อ และ **ไม่ได้วัดแค่คำตอบ** แต่วัดว่าคุณข้ามอะไร ย้อนกลับไปแก้อะไร
อ่านบทความถึงกี่ % และยอมทบทวนคำตอบไหมเมื่อเจอข้อมูลที่สวนกับที่ตอบไป

## 📱 เปิดใช้งาน

| วิธี | ทำยังไง |
|------|---------|
| **บนมือถือ (แนะนำ)** | เปิด GitHub Pages ของ repo นี้ (ดูด้านล่าง) แล้วเข้าเว็บจากมือถือ · กด "เพิ่มลงหน้าจอโฮม" ได้ เพราะมี web manifest |
| **บนเครื่องตัวเอง** | ดับเบิลคลิก `docs/index.html` — ไม่ต้องติดตั้งอะไร ไม่ต้องมีเซิร์ฟเวอร์ |

### เปิด GitHub Pages ครั้งแรก

ไปที่ **Settings → Pages** ของ repo แล้วตั้ง:

- **Source:** Deploy from a branch
- **Branch:** `claude/adaptive-mindset-assessment-web-2wwsck` (หรือ `main` ถ้า merge แล้ว)
- **Folder:** `/docs`

กด Save รอสักครู่ เว็บจะขึ้นที่ `https://metasitwute021.github.io/wute/`

## 🗂️ ไฟล์

| ไฟล์ | คืออะไร |
|------|---------|
| [`ไอเดีย_เว็บทดสอบ_Mindset.md`](./ไอเดีย_เว็บทดสอบ_Mindset.md) | เอกสารไอเดียฉบับเต็ม — แนวคิด, โมเดลรายได้, กลไกแตกกิ่ง, สิ่งที่วัดจากพฤติกรรม, สถาปัตยกรรม, กฎหมาย, roadmap |
| [`items.json`](./items.json) | **คลังข้อคำถาม 120 ข้อ** — ต้นฉบับ ยกไปเข้า backend ได้ทันที |
| [`ITEMS.md`](./ITEMS.md) | ที่มาของข้อ, **ลิขสิทธิ์**, scoring key, วิธีเพิ่มข้อ |
| [`app.html`](./app.html) | โครงเว็บ + engine (มี marker ให้ฉีดคลังข้อ) |
| [`build.py`](./build.py) | ตรวจคลังข้อ แล้ว build ออกมาที่ `docs/` |
| `../docs/` | **ผลลัพธ์ที่ deploy จริง** — `index.html` ไฟล์เดียวจบ + manifest + icon |

## 🔧 แก้แล้ว build ใหม่

```bash
python3 build.py           # ตรวจคลังข้อ + build ไป docs/
python3 build.py --check   # ตรวจอย่างเดียว ไม่เขียนไฟล์
```

แก้ **คำถาม** ที่ `items.json` · แก้ **หน้าตาเว็บหรือ engine** ที่ `app.html` · **อย่าแก้ `docs/index.html` ตรง ๆ** เพราะจะโดนทับทุกครั้งที่ build

`build.py` จะไม่ยอม build ถ้าคลังข้อผิดกติกา เช่น เส้นบังคับชี้ไปข้อที่ไม่มีอยู่, แกนไหนมีข้อไม่ถึง 12 ข้อ, ข้อกลับด้านน้อยกว่า 40%, forced-choice น้อยกว่า 12 ข้อ หรือข้อเปิดน้อยกว่า 8 ข้อ

## ✅ เว็บนี้ทำอะไรได้แล้ว

- **แตกกิ่งจริง** — คลัง **135 ข้อ** เจอจริง 18–26 ข้อ (จำลอง 500 คน ได้เส้นทางไม่ซ้ำกันเลย 500/500)
- **เลือกข้อถัดไปแบบ adaptive 4 ชั้น** — เส้นบังคับ → โควตาข้อบทความ → โควตาขั้นต่ำต่อแกน → แกนที่หลักฐานน้อยสุด แล้วสุ่มในกลุ่มที่ดีเท่ากัน
- **จบเองได้ก่อนครบ 20 ข้อ** เมื่อทุกแกนข้อมูลพอ
- **เก็บพฤติกรรม** — ข้าม / ย้อนกลับ / เปลี่ยนคำตอบ / scroll อ่านถึงกี่ % / เวลาก่อนแตะตัวเลือกแรก
- **วัด growth mindset จากปฏิกิริยาจริง** — ยิงข้อมูลสวนกลับหลังตอบ แล้วดูว่ายอมทบทวนไหม
- **แยกสาย A (คะแนนจากคำตอบ) ออกจากสาย B (ข้อสังเกตจากพฤติกรรม)** ไม่ยุบเป็นตัวเลขเดียว
- **สัตว์ประจำตัว 1 ตัว** — จาก 20 ตัวที่แต่ละตัวแทน "แรงขับ" คนละอย่าง (สิงโต=อำนาจ · จิ้งจอก=กลยุทธ์ · นกยูง=การยอมรับ · เต่า=ความมั่นคง ฯลฯ) จับคู่ด้วยการฉายโปรไฟล์ลงบนทิศทางของสัตว์ ไม่ใช่หาระยะใกล้สุด (แบบหลังทำให้คนคะแนนกลาง ๆ ได้ตัวเดียวกันหมด) พร้อมบอกว่าได้ตัวนี้เพราะอะไร — **จงใจไม่ผูกสัตว์ตัวไหนกับ psychopathy โดยตรง** สัตว์คือสัญลักษณ์ ไม่ใช่ฉลากวินิจฉัย
- **กลุ่มงานที่น่าจะเข้ากับคุณ** — แปลง Big Five เป็นความสนใจอาชีพตามกรอบ RIASEC โดยใช้ค่าสหสัมพันธ์จาก meta-analysis เป็นน้ำหนัก และติดป้าย "เดาอ่อน" ให้กลุ่มที่งานวิจัยรองรับน้อย พร้อมบอกตรง ๆ ว่ากลุ่มงานลงมือทำประเมินจาก Big Five ไม่ได้
- **แนวโน้ม Dark Triad ครบสามด้าน** — ขึ้นด้านที่ใกล้เคียงคุณที่สุดก่อนแบบเต็ม (พร้อมข้อดี/ข้อเสี่ยง) แล้วตามด้วยอีกสองด้านแบบย่อ พร้อมกล่องเตือนว่าเป็นแนวโน้มไม่ใช่การวินิจฉัย และห้ามใช้ตัดสินคนอื่นหรือคัดคนเข้าทำงาน (ดูกติกาเต็มใน `ITEMS.md`)
- **ผังห้าเหลี่ยม Big Five** — SVG วาดเอง ไม่มี dependency ยิ่งไกลจากกึ่งกลาง = ยิ่งไปทางขั้วนั้นมาก มีเส้นประบอกกึ่งกลาง และจุดกลวงบอกแกนที่ข้อมูลยังน้อย · แสดงทั้งในหน้าผลและในการ์ดสรุปรวม
- **การ์ดสรุปรวมเต็มจอ** — กดปุ่ม "ดูสรุปรวม" แล้วเห็นทุกอย่างสำคัญในจอเดียว (3 แกนที่เด่นสุด + มุมมองต่อความสามารถ + สิ่งที่ทำระหว่างตอบ) เหมาะกับแคปหน้าจอ ปิดด้วยปุ่มหรือ Esc
- **อ่านผลเป็นประโยค ไม่ต้องเดาจากจุดบนเส้น** — ทุกแกนมีบรรทัดสรุปเช่น "ค่อนไปทาง \u201cวางแผน มีวินัย\u201d" อยู่เหนือแถบ
- **หน้าผลโฟกัสที่คะแนน** — ข้อสังเกตพฤติกรรม / เส้นทางที่เดิน / log ดิบ พับเก็บไว้ กดกางเมื่ออยากดู
- **คำสั่งเจนภาพ** — ล่างสุดของหน้าผลมีปุ่มคัดลอกคำสั่ง (ภาษาอังกฤษ) ที่ประกอบจากผลจริงของผู้ใช้ เอาไปวางในเครื่องมือเจนภาพได้เลย · **ในอนาคตตรงนี้จะกลายเป็นภาพที่ AI เจนให้อัตโนมัติแทนไอคอนสัตว์** จุดที่จะเสียบ API คือฟังก์ชัน `imagePrompt()` ใน `app.html`
- **โปร่งใส** — โชว์ event log ดิบทั้งหมดให้ผู้ใช้ดูเองที่หน้าผล
- รองรับโหมดสว่าง/มืด + ปุ่มสลับ, ออกแบบมาเพื่อจอมือถือเป็นหลัก

## ⛔ ยัง *ไม่* ทำ

- **ไม่มีคลิป** — schema รองรับไว้แล้วแต่ยังไม่ได้ทำ UI เล่นคลิป
- **ไม่มี backend** — ทุกอย่างอยู่ในหน่วยความจำเบราว์เซอร์ ปิดหน้าต่างแล้วหายหมด ไม่ส่งข้อมูลออกไปไหน
- **ไม่มีระบบจ่ายเงินจริง** — ปุ่มบริจาคเป็นของปลอม
- **คะแนนยังไม่ใช่ผลทางจิตวิทยาที่เชื่อถือได้** — ต้องทำ Phase 2 (เก็บข้อมูลจริง 200–500 คน) ก่อน ดู `ITEMS.md`

> ⚠️ ตอนทำระบบจริง **ห้ามส่งคลังข้อทั้งก้อนมาที่เบราว์เซอร์** แบบที่เดโมทำ — เปิด DevTools ก็เห็นน้ำหนักคะแนนทุกข้อ ต้องคำนวณข้อถัดไปฝั่งเซิร์ฟเวอร์และส่งมาทีละข้อ
