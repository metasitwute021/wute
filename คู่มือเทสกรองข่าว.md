# 📋 คู่มือเทส EA + กรองข่าว (Backtest) — ฉบับละเอียด

> เป้าหมายคืนนี้: เทส **v2.12 (LET-RUN) + กรองข่าว** 6.5 ปี ดูว่า DD ลดไหม
> ถ้า DD ลดสวย → อัปเกรดบัญชีจริง / ถ้าไม่ → ใช้ v2.10 สอบ

---

## 🗂️ ส่วนที่ 1: เตรียมไฟล์ข่าว (เลือก 1 ใน 2 วิธี)

### ✅ วิธี A — ใช้ Script ดึงเอง (แม่นสุด แนะนำ!)
ได้ข่าว **ครบทุกตัว** (NFP, CPI, FOMC, PMI...) + **ไม่ต้องปวดหัวเรื่อง timezone**

1. เอา `FF_News_Exporter.mq5` ไปไว้ `MQL5/Scripts/` → compile (F7)
2. ต่อเน็ตโบรก + เปิดแท็บ **Calendar** ใน MT5 สักครั้ง (ให้โหลดข่าว)
3. ลาก Script ลงกราฟ → ตั้งค่า: from `2020.01.01`, to `2027.01.01`, USD, High+Medium=true
4. กด OK → ได้ไฟล์ `ff_news.csv` ใน `MQL5/Files/` อัตโนมัติ
5. **offset = 0** (ไม่ต้องปรับ เพราะใช้เวลา MT5 ฐานเดียวกัน)

### 🟡 วิธี B — ใช้ไฟล์ NFP สำเร็จรูป (สำรอง ถ้า A มีปัญหา)
ไฟล์ `ff_news_NFP_starter.csv` (ผมทำให้แล้ว มีแต่ NFP 2020-2026, 84 ตัว)
- มีแค่ NFP (ข่าวแรงสุด) → เห็นผลคร่าวๆ แต่ไม่ครบเท่าวิธี A
- เวลาเป็น **19:30 (เวลาไทย)** → ต้องตั้ง offset ให้ตรง server (ดูส่วนที่ 3)

---

## 🗂️ ส่วนที่ 2: วางไฟล์
1. MT5 → **File → Open Data Folder**
2. เข้า `MQL5` → `Files`
3. วางไฟล์ CSV ที่นี่ (ทั้ง 2 วิธีวางที่เดียวกัน)

---

## ⚙️ ส่วนที่ 3: ตั้งค่า EA (กลุ่ม 📰 NEWS FILTER)

| ช่องในหน้า Inputs | ตั้งเป็น | หมายเหตุ |
|---|---|---|
| `Forex Factory CSV file` | **true** | เปิดกรองข่าวจากไฟล์ |
| `file in MQL5/Files` | `ff_news.csv` (หรือ `ff_news_NFP_starter.csv`) | ชื่อไฟล์ที่วาง |
| `impacts to block` | `High,Medium` | บล็อกแดง+ส้ม |
| `shift CSV times to server time` | **0** (วิธี A) | วิธี B ต้องหา offset เอง* |
| `also CLOSE positions` | false (ก่อน) | ลองปิดไม้ก่อนข่าวทีหลังได้ |

> *หา offset (วิธี B): ดูแท่ง NFP (ศุกร์แรกของเดือน) ว่าราคากระชากตอน server กี่โมง แล้วเทียบกับ 19:30 ในไฟล์ → ส่วนต่างใส่ในช่องนี้

✅ **เช็กว่าโหลดสำเร็จ:** ตอนเปิด EA ต้องขึ้น `📰✅ Forex Factory news loaded: XX events`

---

## 🧪 ส่วนที่ 4: ตั้งค่า Backtest (Strategy Tester)

| ช่อง | ตั้งเป็น |
|---|---|
| Expert | `Metasit_XAUUSD_Donchian_EA_RUN` (v2.12 LET-RUN) |
| Symbol | XAUUSD (ตัวที่มีข้อมูลยาว) |
| Period (TF) | H4 |
| Date | 2020.01.01 → 2026.06.01 (6.5 ปี) |
| Model | Every tick (หรือ 1 minute OHLC ก็พอ) |
| Deposit | 100000 (เทสหา DStruct ใช้ทุน $100k) |
| Leverage | ตามโบรก |

### 🔧 ตั้งค่า INPUTS ของ EA สำหรับเทส 6.5 ปี (ปิดเบรก เพื่อดู DD ดิบ)

| ช่อง | ค่าเทส | เหตุผล |
|---|---|---|
| `Risk per trade (%)` | **1.0** | ตามที่อยากใช้ |
| `Reach this % profit -> STOP` | **0** | ไม่ให้หยุดกลางคัน |
| `EMERGENCY BRAKE` | **0** | ดู DD ดิบ |
| `DD LOCK from peak` | **0** | - |
| `Max drawdown -> pause (%)` | **100** | - |
| `Daily drawdown -> stop (%)` | **100** | - |
| `Skip trade if risk > %` | **0** | - |
| `Close + block before weekend` | **false** | - |
| `Forex Factory CSV file` | **true** | ← เปิดข่าว! |

---

## 🔬 ส่วนที่ 5: รัน A/B เปรียบเทียบ (หัวใจของคืนนี้)

รัน **2 รอบ เหมือนกันทุกอย่าง เปลี่ยนแค่ข่าว:**

| รอบ | `Forex Factory CSV file` | ดู |
|---|---|---|
| A | **false** (ไม่กรองข่าว) | = ผลเดิม v2.12 (DD 16.95%) |
| B | **true** (กรองข่าว) | DD ลดลงไหม? |

---

## 🚦 ส่วนที่ 6: เกณฑ์ "ไฟเขียว" (ตัดสินใจ)

เทียบรอบ B กับรอบ A:

| เช็ก | ผ่านเมื่อ |
|---|---|
| Max DD | **ต่ำลง** กว่า 16.95% |
| Profit Factor | ยังเกิน **1.2** |
| ขาดทุนติดกัน (consecutive) | ไม่แย่ลง |

- ✅ ผ่านหมด → ข่าวช่วยจริง → **อัปเกรดได้** (แล้วลด risk เป็น 0.3-0.5% ตอนใช้จริง)
- ❌ ไม่ผ่าน → ใช้ **v2.10** สอบ, เก็บ v2.12 ไว้ตอน funded

---

## 📌 สรุปไฟล์ที่ต้องมี
1. `Metasit_XAUUSD_Donchian_EA.mq5` (v2.13 — BANK-EARLY) → `MQL5/Experts/`
2. `Metasit_XAUUSD_Donchian_EA_RUN.mq5` (v2.13 — LET-RUN) → `MQL5/Experts/`
3. `FF_News_Exporter.mq5` → `MQL5/Scripts/`
4. ไฟล์ข่าว CSV → `MQL5/Files/`

> compile ทุกไฟล์ (F7) ก่อนใช้ทุกครั้ง 👍
