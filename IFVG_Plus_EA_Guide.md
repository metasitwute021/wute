# IFVG+ EA — คู่มือโครงสร้างโค้ด

> สรุปหน้าที่แต่ละส่วนของไฟล์ **`IFVG_Plus_EA.mq5`**
> Inversion Fair Value Gap Expert Advisor · MetaTrader 5 · เวอร์ชัน 1.00

**ภาพรวม:** เปรียบ EA เป็น “พนักงานเทรด 1 คน” ที่มีหน้าที่ย่อยหลายอย่าง หัวใจคือการหาช่องว่างราคา (FVG) แล้วรอให้ราคา “ปิดทะลุกลับ” (Inversion) จึงเข้าเทรด ตั้ง SL ใต้แท่งที่ 3 ไม่มี TP ปล่อยกำไรวิ่งด้วย trailing พร้อมระบบป้องกันพอร์ตแบบ Prop-firm และการวาดกล่อง FVG บนกราฟ

---

## โค้ดแบ่งเป็น 7 กลุ่มใหญ่

| กลุ่ม | หน้าที่โดยรวม |
|------|--------------|
| 1. Inputs | ปุ่มตั้งค่า — ปรับได้โดยไม่ต้องแก้โค้ด |
| 2. Globals | สมุดจดความจำของ EA ระหว่างทำงาน |
| 3. วงจรชีวิต | OnInit / OnTick / OnTimer / OnDeinit |
| 4. IFVG Engine | สมองหาสัญญาณเข้าเทรด (หัวใจ) |
| 5. วาดกล่อง | แสดง FVG และลูกศรบนกราฟ |
| 6. ระบบป้องกัน | Prop guards / Drawdown / ข่าว / ประตูอนุญาตเทรด |
| 7. บริหารไม้ | Lot / SL / Trailing / ปิดบางส่วน |

---

## 1. Inputs — ปุ่มตั้งค่า

ส่วนที่ขึ้นต้นด้วย `input` คือค่าที่ปรับได้จากหน้าต่าง EA ตอนลากใส่กราฟ โดยไม่ต้องแก้โค้ด

| กลุ่ม Input | คุมอะไร |
|------------|---------|
| General / Timeframes | Magic number, แจ้งเตือน, ไทม์เฟรมสัญญาณ (M15) และเทรล (M5) |
| IFVG detection | ช่องกว้างกี่จุด, แท่งกลางแรงแค่ไหน, FVG อายุกี่แท่งถึงทิ้ง |
| '+' filters | ฟิลเตอร์เสริม: เทรนด์ EMA และเซสชันเวลา |
| Risk / sizing | ความเสี่ยง%, **Risk cap**, buffer SL, **spread guard**, **cooldown** |
| Trade management | Break-even, ปิดบางส่วน |
| Trailing | ตั้งค่า ATR/swing/แท่งที่ 3 |
| Prop-firm protection | **Profit target, Emergency brake**, Max DD, Daily DD |
| Weekend guard | ปิดไม้ + งดเทรดสุดสัปดาห์ |
| News filter | กรองข่าวแรง |
| Visualization | สีกล่อง, เปิด/ปิดการวาดกล่องและลูกศร |

---

## 2. Globals — สมุดความจำ

| ตัวแปร | เก็บอะไร |
|--------|---------|
| `trade` | เครื่องมือส่งคำสั่งซื้อขาย |
| `hEMA, hATR...` | ตัวเชื่อมไปอ่านค่าอินดิเคเตอร์ |
| `gZones[]` | ★ ลิสต์ FVG ที่เจอแล้วแต่ยังไม่ inversion (รออยู่) |
| `gPosTicket[]...` | จำสถานะไม้ที่เปิด (เคย BE แล้วยัง? ปิดบางส่วนยัง?) |
| `gStartBalance` | ทุนตั้งต้น = เส้นอ้างอิงของกองทุน (prop) |
| `gHardStopped` | ธงหยุดถาวร เมื่อโดน profit target / emergency brake |
| `gPeakEquity, gInNews, gCooldownBars...` | สถานะระบบป้องกัน |

---

## 3. วงจรชีวิต — หัวใจการหมุนเวลา

| ฟังก์ชัน | ทำงานเมื่อ | หน้าที่ |
|---------|-----------|--------|
| `OnInit()` | ลาก EA ใส่กราฟ | สร้างอินดิเคเตอร์, จำทุนตั้งต้น, ตั้งนาฬิกา, เคลียร์กล่องเก่า |
| `OnTick()` | ทุกครั้งราคาขยับ | เช็ค prop guard → จัดการไม้ → หาสัญญาณใหม่เมื่อขึ้นแท่งใหม่ |
| `OnTimer()` | ทุก 5 วินาที | เช็ค prop / Drawdown / weekend / ข่าว |
| `OnDeinit()` | ถอด EA ออก | ลบกล่อง/ลูกศร, คืนหน่วยความจำ |

> **จุดสำคัญใน OnTick:** `CheckPropTargets()` ทำงาน **อันดับแรกสุด** — ถ้าโดน hard stop จะปิดทุกไม้และหยุดทันที จากนั้นจึงจัดการไม้ที่เปิด และหาสัญญาณเข้าเฉพาะตอนขึ้นแท่งใหม่

---

## 4. IFVG Engine — สมองหาสัญญาณ (หัวใจ)

| ฟังก์ชัน | หน้าที่อธิบายง่าย ๆ |
|---------|------------------|
| `DetectNewFVG()` | มอง 3 แท่งล่าสุด — ถ้าหาง Low แท่งซ้ายไม่ทับหัว High แท่งขวา = เจอช่องว่าง → จดลง `gZones[]` (เช็คก่อนว่าช่องกว้างพอ + แท่งกลางแรงพอ) |
| `ScanForInversion()` | ไล่ดู FVG ที่จดไว้ — ถ้าราคาปิดทะลุกลับผ่านช่อง = inversion → ส่งไปเข้าเทรด |
| `PassesPlusFilters()` | ก่อนเข้าจริง เช็ค “+” : อยู่ฝั่งเดียวกับเทรนด์ EMA ไหม? อยู่ในเซสชันไหม? |
| `AgeAndPruneZones()` | นับอายุ FVG — ถ้าเก่าเกินไป หรือราคาทะลุผิดทาง = ลบทิ้ง |

---

## 5. การวาดกล่อง — แสดงผลบนกราฟ

| ฟังก์ชัน | หน้าที่ |
|---------|--------|
| `DrawZone()` | วาดกล่องคลุมโซน FVG (แดง=รอ Buy / ฟ้า=รอ Sell) |
| `ExtendZones()` | ยืดขอบขวากล่องตามเวลาจริงตราบใดที่ FVG ยังไม่ถูกใช้ |
| `DrawEntryArrow()` | ปักลูกศรเขียว/แดงตรงจุดเข้าไม้ |
| `DeleteAllObjects()` | ลบกล่อง/ลูกศรทั้งหมด |

---

## 6. ระบบป้องกัน — ยามเฝ้าประตู

| ฟังก์ชัน | หน้าที่ |
|---------|--------|
| `CheckPropTargets()` | ★ เป้ากำไร +6% / เบรกฉุกเฉิน −3.5% → ปิดหมด + STOP |
| `CheckDrawdownProtection()` | ขาดทุนรวม 3% → พัก EA / ขาดทุนวันละ 1.5% → หยุดทั้งวัน |
| `IsNewsBlocking()` | เช็คปฏิทินข่าว — ใกล้ข่าวแรง ±30 นาที = ห้ามเข้าไม้ |
| `IsWeekendBlock()` / `UpdateWeekendGuard()` | ศุกร์เย็น → ปิดไม้ + งดเทรดเสาร์-อาทิตย์ |
| `TradingAllowed()` | ★ ประตูสุดท้าย รวมทุกเงื่อนไข ถ้าไม่ผ่าน EA ไม่เข้าเทรดเด็ดขาด |
| `CloseAllPositions()` | ปิดไม้ทั้งหมดของ EA ตัวนี้ |

---

## 7. บริหารไม้ — เมื่อเข้าเทรดแล้วทำอะไรต่อ

| ฟังก์ชัน | หน้าที่ |
|---------|--------|
| `OpenTrade()` | เช็ค spread → ส่งคำสั่ง Buy/Sell จริง พร้อมตั้ง SL (ไม่มี TP) |
| `CalcLotSize()` | ★ คำนวณ Lot จาก Risk% ÷ ระยะ SL + **Risk cap** (รองรับพอร์ต Cent) |
| `NormalizeVolume()` | ปัด Lot ให้ตรงขั้นต่ำของโบรกเกอร์ |
| `SyncPositionState()` | อัปเดตลิสต์ไม้ที่เปิด/ปิด + ตั้ง cooldown เมื่อปิดไม้ |
| `ManageOpenPositions()` | ★ ทุก tick: เลื่อน BE → ปิดบางส่วน 40% ที่ 1R → เทรล |
| `ComputeTrailingSL()` | หา SL เทรล “ปลอดภัยที่สุด” จากหลายชั้น (ATR + swing + แท่งที่ 3) |
| `ModifySL()` | ขยับ SL ได้เฉพาะทางแน่นขึ้น ห้ามถอยหลัง |

---

## สรุปการไหลของงาน (1 รอบสมบูรณ์)

```
ราคาขยับ -> OnTick
   |- CheckPropTargets()  (ถึงเป้า +6%? / เบรก -3.5%? -> ปิดหมด STOP)
   |- ManageOpenPositions()   (มีไม้อยู่? เทรล/BE/ปิดบางส่วน)
   |- ขึ้นแท่งใหม่?
        |- UpdateCooldown()   (นับถอยหลัง cooldown หลังปิดไม้)
        |- AgeAndPruneZones() (เก็บกวาด FVG เก่า)
        |- DetectNewFVG()     (เจอช่องใหม่? จดไว้ + วาดกล่อง)
        |- ScanForInversion() (ราคาทะลุกลับ? -> เข้าไม้ + ปักลูกศร)
        |- ExtendZones()      (ยืดกล่องที่ยังรออยู่)
```

---

## 8. เกราะความปลอดภัย (Prop-firm)

ส่วนที่เสริมเข้ามาจากบทเรียนการพัฒนา EA ทอง (Donchian) — เพื่อให้ปลอดภัยพอจะใช้กับบัญชีสอบกองทุน

| เกราะ | การทำงาน | Input / ค่าเริ่มต้น |
|------|---------|-------------------|
| **A1 Risk Cap** | ถ้า lot ขั้นต่ำเสี่ยงเกินเพดาน → ไม่เปิดไม้ (กันแตก ไม้เดียวไม่ทำสอบตก) | `InpMaxRiskCapPercent = 1.7%` |
| **A2 Profit Target** | ถึง +% ของทุนเริ่มต้น → ปิดหมด + STOP (ล็อกผ่านสอบ) | `InpProfitTargetPercent = 6.0%` |
| **A2 Emergency Brake** | ขาดทุนถึง −% ของทุนเริ่มต้น → ปิดหมด + STOP (hard) | `InpMaxTotalLossPercent = 3.5%` |
| **A2 Max DD** | ขาดทุนรวมถึง % → พัก EA (แก้จาก 20% เป็น 3% ให้แน่นพอกับเส้นกองทุน) | `InpMaxDD_Percent = 3.0%` |
| **A3 Spread Guard** | spread กว้างเกิน → ข้ามการเข้า (กันช่วงข่าวทอง spread บาน) | `InpMaxSpreadPoints = 0` (ปิด) |
| **A3 Cooldown** | รอกี่แท่งหลังปิดไม้ก่อนเข้าใหม่ (กัน whipsaw) | `InpReentryCooldownBars = 0` (ปิด) |
| **B5 Weekend Guard** | ศุกร์เย็น → ปิดไม้ทั้งหมด + งดเข้าเสาร์-อาทิตย์ (กัน Monday gap) | `InpWeekendGuard = true` |

> **เส้นตายกองทุน = 5% static จากเงินตั้งต้น** → Emergency Brake ที่ 3.5% หยุดก่อน เหลือ buffer ~1.5% กัน slippage/gap · **Hard stop = ต้อง re-init (ถอด/ใส่ EA ใหม่) ถึงจะรันต่อ** → บังคับวินัย ไม่เทรดฝืน

---

## 9. บทเรียนที่ยึด (Money Management)

| หลักการ | เหตุผล |
|--------|--------|
| **ใช้ Fix Risk % เท่านั้น** | Sharpe ดีสุด (พิสูจน์แล้ว) — ตัว EA นี้ใช้แนวนี้ล้วน |
| **ห้าม Martingale** | เบิ้ลหลังแพ้ = ล้างพอร์ตแน่นอน |
| **ห้าม Confidence / Anti-Martingale** | เทสจริงพบว่าฉุดผลงาน (PF 0.80 vs 1.11) → จึงไม่ใส่ในตัวนี้ |
| **ปล่อยกำไรวิ่ง (R:R)** | อย่าตัด winner เร็ว — trailing ช่วยดัน PF + ลด DD |
| **SL คือเกราะจริง** | News filter จับได้แค่ข่าวในตาราง ข่าวด่วน/สุดสัปดาห์จับไม่ได้ |

---

## ⚠️ ข้อควรทราบ

1. **IFVG ยังไม่มีหลักฐานว่ามี edge** — logic สวย ≠ ทำเงินได้ → ต้อง backtest พิสูจน์ PF/DD ก่อน (เหมือนที่ทำกับ EA ทอง 6.5 ปี)
2. ค่าเริ่มต้นจูนมาเพื่อ **XAUUSD M15** → ต้องจูน MinGap / Displacement / MaxAge ตามโบรกเกอร์
3. **Backtest สั้นหลอกตา** — 4 เดือน PF 7.3 / win 95% แต่ 6.5 ปีจริง PF 1.2 / win 60% → ดู PF + Max DD + R:R ไม่ใช่แค่กำไร
4. News filter มักไม่ทำงานใน Strategy Tester (เมื่อดึงปฏิทินไม่ได้ EA จะไม่บล็อก)
5. **ควร Compile (F7) ใน MetaEditor และทดสอบบนเดโมก่อนใช้งานจริงเสมอ**

---

*IFVG+ EA v1.00 — `IFVG_Plus_EA.mq5`*
